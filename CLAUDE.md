# Learn — personal learning app (Ollie, SBI student → robotics)

One shared knowledge graph across CS/math/physics/engineering/robotics. MVP: full CS skill tree + module 1 "How Computers Work" fully playable. Long-term: every domain grows to FULL bachelor's-degree depth (one graph, cross-subject links), robotics as the converging goal.

Project lives at `~/Claude/Projects/Learning website` (moved from "GSP IoT" — open sessions HERE).

## Stack
Vite + React + TS · Tailwind v4 (@tailwindcss/vite, no config file) · zustand+persist (one versioned localStorage blob) · @xyflow/react (skill tree AND gate sandbox) · motion (unlock/completion animations only). No backend, no router.

## Layout
- `src/types.ts` — ALL types (KnowledgeNode, Screen union, Progress)
- `src/content/curriculum.ts` — knowledge graph, hand-authored x/y positions
- `src/content/lessons/*.ts` — one file per lesson node
- `src/components/` — SkillTreeView, LessonPlayer, GateSandbox, screen components
- `src/store.ts` — zustand store; node status (locked/available/mastered) computed via selector, never stored

## Screen union (discriminated on `kind`)
`explain` | `predict {question, options, reveal}` | `quiz {question, options, correctIndex, explanations[]}` | `gatePuzzle {puzzleId}` | `sandbox {presetId?}`

## Pedagogy (non-negotiable — this IS the product)
1. Max ~3 sentences prose before the learner must DO something.
2. Predict → observe → explain before any sim reveals a result.
3. Quizzes answered from memory; every wrong option gets a specific misconception explanation.
4. Suggested-path gating (SOFT — changed by Ollie's request): nothing is ever locked. Nodes with unmet prereqs show as 'later' (🕒, dimmed) with a "Suggested first" list and a de-emphasized "Start anyway →". Any node can be marked "I already know this" (⏩ `knownNodeIds`, reversible everywhere, earns NO XP) which satisfies downstream suggestions. Mastery (quiz ≥ 80%) remains the only source of ✅/XP; real mastery supersedes a known-mark.
5. Worked-example fading: full example → completion step → independent.
6. XP only for mastery. No streaks/leagues. Celebrate unlocks with animation.
7. Every concept: one-line real-world/robotics hook (whyItMatters).

## Quality gate (run EVERY time you add/change content)
1. `npm run build` (this is the real typecheck — `tsc -b`).
2. Content validator: `src/content/validate.ts` runs automatically on dev boot and logs to the browser console: `[content-validation] ✓ all content valid` or a list of problems. It checks duplicate ids, prereq/link targets exist, quiz explanations==options, gameId/puzzleId resolve, lessons end with a quiz, puzzle truth-table widths, resource URLs. ADD a new invariant here whenever a new content rule matters.
3. For any new interactive game/sim, browser-verify it actually completes (drive it via preview). For sims with a win condition, also confirm it's WINNABLE (e.g. PID: numeric node script).
4. Fact-check technical claims by hand (validator can't). All current lessons hand-verified.

## Feature notes (adaptive tempo, interlinking, theming, spaced repetition)
- **Interlinking + mid-session save**: any Screen can carry `links: LessonLink[]`. Clicking a link calls `jumpToLesson` (pushes a return entry onto `navStack`, persisted), opens the target lesson; a "↩ return" banner + the results "Back to X" button call `exitLesson` which pops the stack. Lesson position is saved per node in `lessonProgress` (persisted) and resumed on reopen (quiz section restarts fresh to keep scoring whole).
- **Adaptive tempo**: `ExplainScreen.deeper?: string[]` → "Go deeper" toggle. `Lesson.extraPractice?: QuizQuestion[]` → "Extra practice" button on the results screen (ungraded reinforcement).
- **Light/dark mode**: `store.theme` toggled in header; App applies `light`/`dark` class to `<html>`. Light mode = `html.light` block in index.css that REVERSES the slate ramp + darkens accent-300 text (Tailwind v4 emits `var(--color-*)`, so this re-themes with zero component edits). React Flow `colorMode` follows theme.
- **Soft gating / "I already know this"**: `NodeStatus` = `later | available | known | mastered` (no more `locked`); `nodeStatus()` takes `knownNodeIds` as a 4th arg. Store: `knownNodeIds` (persisted) + `markKnown`/`unmarkKnown`; `masterNode` removes a known-mark when the node is truly mastered. UI: map icons 🕒 (later, dimmed) / ⏩ (known, dashed border); DetailPanel "Suggested first" list has per-prereq "know it"/"↩ undo" chips, later-status nodes get an amber nudge + "Start anyway →", available nodes get "⏩ Too easy? Mark as already known", known nodes get a banner + "↩ back to normal". LessonPlayer's results "Up next on your path" treats known as satisfied. Reviews are NOT scheduled for known-marks (only real mastery). Browser-verified end-to-end incl. persistence + reversal.
- **Map VIEW TIERS (the stage-scalable layer selector)**: the app is named **Atlas** (renamed from "Foundations" — the old name read as a mode label once view modes existed; App.tsx header + index.html title). Every node has a content layer: `nodeTier(n)` in curriculum.ts = explicit `n.tier` ?? (`isDepthId(id)` i.e. `/-\d$/` ⇒ 1 : 0). `viewTiers` (curriculum.ts) lists the selectable layers — currently 🌱 Essentials (0) and 🌳 Depth (1); **when a future stage's nodes land (rigor=2, labs=3…), ADD AN ENTRY to `viewTiers` + set `tier` on the new nodes and the map's segmented control picks it up automatically**. `store.viewTier` (persisted, default MAX_TIER = show everything, clamped in `setViewTier`) selects cumulatively: tier ≤ viewTier is shown. Hidden higher-tier nodes get their edges re-anchored down the chain via `projectToTier` (walk `prereqIds[0]` while tier > T); one edge list per tier precomputed at module level (`edgesByTier`) through the shared `buildVisibleEdges` (same transitive-redundancy filter). VIEW-only — `nodeStatus`, suggestions and the detail panel always use the full graph. `importProgress` accepts the short-lived legacy `showDepth` boolean. Browser-verified: 0 vs 20 depth nodes visible, edges reconnect, refit on switch.
- **Progress panel (stats + achievements)**: lives in `ProgressPanel.tsx` — the header **⚡ XP badge IS the trigger**: it's a button that toggles a dropdown (ReviewQueue header-chip/outside-click-to-close pattern), moved there 2026-07-08 (Ollie: "move the progress to the XP in the header"). Panel = 6 headline stat cards, per-domain mastery bars (subject-colored), 14 achievements, and a bottom "Reset all progress" button (window.confirm-guarded) — ALL derived on the fly from existing Progress fields via `src/content/achievements.ts` (`computeStats`/`computeAchievements`), nothing new persisted. SettingsPanel (⚙︎) is now Data-only (Export/Import/Reset — no more tabs). "Reviews completed" = reviews with `intervalDays > REVIEW_FIRST_INTERVAL_DAYS`; "Deep Diver" = any intro+-2+-3 chain fully mastered. When adding achievements keep them derivable — no new persisted counters. Browser-verified: XP click opens panel, outside-click closes, Settings shows only Data.
- **Achievement toasts**: `AchievementToasts.tsx` (mounted in App's `<main>`) watches the 5 progress fields, recomputes unlocked achievements, and pops an amber motion-animated toast (bottom-center, 6s auto-dismiss, click to dismiss) for each NEWLY unlocked one. Session-scoped `seen` ref: first compute snapshots without announcing (no toast spam on load); unlocking after a reset re-announces. Toasts carry unique instance keys so one toast's dismiss timer can't remove another. Browser-verified (drop below Double Digits threshold → re-master 10th node → toast).
- **Roadmap button toggles**: clicking 🧭 Roadmap again closes the overlay (active state = cyan tint, same pattern on the ⚙︎ button).
- **Dev store handle**: `window.__store` (DEV only, set in store.ts) — browser-driven verification MUST use this to reach the live store; `import('/src/store.ts')` in preview_eval gets a PARALLEL zustand instance under Vite HMR (the page's copy is `?t=`-stamped) and mutations won't reach the UI.
- **Spaced repetition (Stage 4 MVP)**: `Progress.reviews: Record<nodeId, {lastSeen, intervalDays}>` — seeded at mastery (3 days), interval DOUBLES per successful review (cap 60d), RESETS to 3 on a failed retake. A review = reopening the mastered lesson and re-passing its quiz (ResultsScreen fires `completeReview(nodeId, passed)` when `alreadyMastered`) OR finishing the whole extraPractice pool (early "exit practice" does NOT count — ExtraPractice has separate onExit/onFinish). Due = now ≥ lastSeen + intervalDays (selector `dueReviewIds` in store.ts). UI: amber "🔁 N due" header chip (ReviewQueue.tsx, dropdown → openLesson) + due map nodes swap ✅→🔁 with an amber ring. Legacy blobs (no `reviews` key): zustand shallow-merge defaults to {}, and `seedMissingReviews()` (called on ReviewQueue mount) backfills mastered nodes at lastSeen=now — no persist migration needed. Reviews earn no XP (XP only for mastery). Browser-verified: backdated review → chip+badge appear → replay quiz 5/5 → interval 3→6d, chip clears.

## Conventions
- Extend working files; small targeted diffs; one game = one file with `{ onComplete(score) }`.
- Content ONLY in src/content — components never hardcode lesson text.
- Definition of done per phase: `npm run build && npx tsc --noEmit` clean, git commit.
- Node.js lives at `~/.local/node/bin` (not on default PATH) — prepend `export PATH="$HOME/.local/node/bin:$PATH"` in shell commands.
- NOTE: `npx tsc --noEmit` alone does NOT catch errors (root tsconfig only references projects) — the real check is `npm run build` (runs `tsc -b`).
- Dark theme, vivid accent per subject: cs=cyan, math=violet, physics=amber, engineering=emerald, robotics=rose.
- The `sandbox` Screen kind is defined but unused so far (adder lessons used gatePuzzles + games instead); free-play sandbox is a future feature.

## Deploy (manual — gh CLI not installed)
1. Create an empty repo on github.com (e.g. `learning-website`, private is fine). No README/gitignore.
2. In `~/Claude/Projects/Learning website`:
   `git remote add origin https://github.com/<username>/learning-website.git && git push -u origin main`
   (or `master` — check `git branch`).
3. On vercel.com: Add New → Project → import the repo. Vercel auto-detects Vite. Build command `npm run build`, output `dist` (defaults). Deploy.
4. Every future `git push` auto-deploys.

## Newer conventions (added with the comprehensiveness pass)
- `resources?: Resource[]` on KnowledgeNode → "Go deeper" links (video/interactive/article/book/course) in the detail panel; curated in `resourcesByNode` at the bottom of curriculum.ts.
- `optional?: boolean` on any Screen → renders a "Bonus challenge — skip" button (learner control; never make core pedagogy optional).
- `isExam?: boolean` on KnowledgeNode → module exam: mixed FRESH retrieval questions (never reuse lesson quiz questions), prereqs = all module nodes.
- Map: jump-to-domain select + whole-map button + subject legend + MiniMap; initial view focuses "How Computers Work".
- Map layout ("STREETS" edition, 2026-07-07 full refactor — built so depth chains keep fitting): three horizontal BANDS by dependency depth with node-free STREETS between them where long edges sweep horizontally. Band 0 y0-1100 foundations: Programming x0-750 | Math x1120-2430 (logic/linalg/prob/calc+ODE columns) | HCW x2720-3190 | Physics x3500-4570 | History/Chemistry islands x4950+. Street S1 y~1100-1400 (sweep lane for prog-data-3→{os,ros} + cpu→os; only math-exam sits below it at (1500,1560), threaded between the two vertical math→ai lanes x1776/x2056). Band 1 y1400-2200: Networks x0 | Algo staircase x620-1310 | Theory x1360 | Robotics convergence cell x2280-3910 | OS x3960-4430. Band 2 y2300-3400: Security x0 | DB x300 | AI x1120-1870 | robo estimation chain + robo-exam bottom-right. KEY structural choice: **HCW sits NEXT TO Physics**, so cpu→robo-sensing is short; the only cross-map corridors are prog-data-3→{os-processes,robo-ros}. **RESERVED SLOTS for queued chains are documented in the header comment of curriculum.ts** (linalg-4/5 → x1400 col; math-mv → x2520 top; math-stats → x2520 below it; robo-control-4 → (2950,2300); phys-statics → x4660; Electronics domain → x4660+/y850+; logic-2/3 → below logic). Rules: never place an edge between same-x nodes that aren't vertically adjacent; exams sit at their cell's bottom, offset out of hub out-edge lanes; edge–EDGE crossings in streets are fine, edge–NODE crossings are not. Transitively-redundant prereq edges are HIDDEN on the map only (buildVisibleEdges in SkillTreeView) — prereqIds stay complete for unlock logic + detail panel. When adding nodes: use a reserved slot (or claim new empty space), then run `node scripts/check-map-layout.mjs` — it must report zero crossings in EVERY view tier.

## Roadmap to bachelor depth (honest gap analysis)
The graph is a broad skeleton, NOT yet a bachelor's. A real bachelor ≈ 180 ECTS ≈ thousands of hours; this app is the SPINE that sequences, motivates and TESTS, with the world's best external material (Nand2Tetris / MIT OCW / 3Blue1Brown / CS50 / Ben Eater, already curated per node in `resourcesByNode`) as the muscle. The end goal: Ollie reaches genuinely bachelor-worthy, hands-on understanding across CS + math + physics + chemistry + robotics, converging on being able to start a robotics company. The path is the six explicit STAGES below. Each future chat should pick the current stage, do a slice of it, keep the quality gate green, and commit.

### The six stages to bachelor-worthy content (with explicit goals)
Progress through them roughly in order, but they overlap — you can deepen one domain (Stage 1) while another is still gaining breadth. Each stage says its GOAL (what the learner gains) and DONE-WHEN (the concrete bar to call it finished).

- **Stage 0 — Foundations / breadth.** GOAL: give the learner intuition + vocabulary for the WHOLE map — one interactive lesson per big idea in every domain, so nothing on the map is a mystery and every subject has an on-ramp. DONE-WHEN: every domain has playable intro lessons for its nodes. → **STATUS: COMPLETE** (47 nodes, 14 domains).
- **Stage 1 — Depth (mini-courses + module exams).** GOAL: turn each single-lesson node from "I get the idea" into "I can actually do this" — multiple lessons per key node (concept → worked problems → independent problem sets via bigger `extraPractice` pools), plus a module exam per domain. DONE-WHEN: every domain has an `isExam` node (fresh mixed retrieval, prereqs = all domain nodes), and the robotics-critical nodes (calculus, linalg, probability, control, programming) each span 2–4 lessons instead of one. THIS IS THE CURRENT FRONTIER — start here (see "Next").
- **Stage 2 — Rigor & formalism.** GOAL: the genuinely hard bachelor content — the notation, proofs and derivations a real exam demands, not just intuition. Math: limits/epsilon-delta, chain rule, eigenvalues, Bayes derivations, basic proof technique. Physics: derive the equations, not just use them. CS: formal complexity analysis, automata proofs. DONE-WHEN: a motivated learner could sit and pass a real first/second-year university exam in the covered topics.
- **Stage 3 — Labs & projects.** GOAL: build real artifacts end-to-end, because you only truly know it once you've built it. Candidate capstones: assemble a working CPU in a sim (extends the gate sandbox), a PID-controlled robot in a physics sim, a tiny OS scheduler, a small SQL/query engine, a neural net from scratch in the code runner, a packet router. DONE-WHEN: each major domain has at least one multi-step build project the learner completes.
- **Stage 4 — Retention (spaced repetition + cumulative exams).** GOAL: make knowledge STICK for years, not days — the difference between "studied it" and "knows it." Resurface mastered nodes' `extraPractice` on an expanding schedule (needs a `lastMastered` timestamp in Progress + a due-review queue on the map), let mastery gently decay and refresh, and add cumulative cross-domain exams. DONE-WHEN: the app schedules reviews automatically and a "due today" review flow exists.
- **Stage 5 — Integration & specialization.** GOAL: fuse the domains into the actual target — a roboticist-founder. Cross-domain capstones that thread perception + control + planning into one working robot, plus a **business & entrepreneurship track** (customer discovery, unit economics, IP, go-to-market) so the learning graph literally leads to launching the company. DONE-WHEN: a full-robot integration capstone exists and a founder track is playable.

### Other subjects worth adding (brainstorm — for future expansion)
New Subject values + domains + nodes (data-only is cheap; lessons come later). Ordered by value for the robotics-founder goal. When adding: pick a distinct subject color, add to `Subject` type + `subjectStyles`/`subjectNames` + `subjectHex` + the `html.light` accent overrides, then place nodes in the correct dependency band (see map-layout notes).
1. **Statistics & Data Science** — beyond probability: estimation, regression, hypothesis testing, experiment design. Underpins ML, sensor calibration, and evaluating whether your robot actually works.
2. **Signal Processing** — filtering, Fourier, sampling. Every camera, lidar, IMU and audio stream is a signal; noise removal and sensor fusion live here.
3. **Control Theory (its own domain, deeper than the PID bridge)** — state space, stability, Kalman filters, optimal control. The mathematical core of making robots move precisely.
4. **Electronics & Circuit Design** — beyond Ohm's law: transistors as amplifiers, op-amps, PCB design, power electronics, motor drivers. Bridges physics/chemistry to real robot hardware you can build.
5. **Economics, Business & Entrepreneurship** — markets, unit economics, product, fundraising, IP/patents. The explicit startup track (also the payoff of Stage 5); Ollie is an SBI student, so this doubles as coursework.
6. **Mechanical Engineering / CAD & Manufacturing** — statics, materials strength, gears/linkages, 3D modelling, tolerances, how parts actually get made. A robot is a physical machine before it's code.
7. **Biology & Biomechanics** — how muscles, locomotion and nervous systems work; bio-inspired robotics (legged robots, soft robotics, swarms).
8. **Neuroscience** — perception, learning and motor control in real brains; the inspiration behind neural nets and modern robot learning.
9. **Optics & Photonics** — lenses, cameras, lidar, structured light — the physics of how robots SEE.
10. **Thermodynamics & Heat** — energy conversion, efficiency, cooling. Motors heat up, batteries have limits, compute throttles.
11. **Software Engineering Practice** — version control (git), testing, debugging, architecture, working in teams. How real, reliable robot codebases are actually built and shipped (distinct from Programming Fundamentals).
12. **Philosophy, Ethics & AI Safety** — logic/epistemology, the ethics of automation and autonomous machines, responsibility. A founder building machines that act in the world needs this.
13. **Design & Human-Computer Interaction** — how humans interact with and trust robots; interfaces, safety, usability.
14. **Law & Intellectual Property** — patents, liability, regulation of robots/AI. Practical survival knowledge for a hardware startup.
Also fold in earlier gaps: **electromagnetism** and **advanced calculus/differential equations** (deepen Physics/Math rather than new domains).

## Stage 1 — structure & backlog (the depth stage, now underway)
**The proven chain pattern** (repeat for every entry below): intro node → `-2` → `-3` vertical-ish chain; each depth lesson = concept → worked-fading → independent, a `deeper?` toggle, ONE Node-verified `code` screen, a ≥4-question `extraPractice` pool, curated `resources`. Grow the domain exam by ~4 FRESH questions and add the new nodes to its prereqs (validator enforces). Repoint long downstream corridors to the chain bottom when geometry demands (see checker below). Verify: build + validator + whole-map zero crossings + browser-play one new lesson end-to-end.

**Full syllabi, gap-rankings vs current coverage, live-verified problem sources (with solutions), and curated resource shortlists for every domain live in [docs/curriculum-gap-analysis.md](docs/curriculum-gap-analysis.md)** (Cowork research, fetched 2026-07-06). READ IT before authoring a new chain — it names the exact course units/psets/exams/videos to pull from instead of improvising, and explains the reasoning behind the ranking below.

**Backlog, re-ranked from the gap analysis (robotics-critical + SBI-year-2 leverage first). ✅ = done. This list has grown a LOT relative to the original 5-item backlog — that's real: bachelor depth is a multi-year project. Treat Phase 1 as the concrete near-term queue; Phase 2–4 are the honest longer arc, not "do all of this next." One chain per session is the right cadence.**

Phase 1 — robotics core (do in order):
- ✅ math-linalg / math-prob / math-calculus (3 chains, done)
- ✅ robo-control → tuning → feedforward/state-space (done)
- ✅ programming: prog-data → recursion → debugging/testing (done)
- ✅ **phys-forces chain (DONE, 2-lesson version)**: phys-forces-2 "Kinematics: suvat, Projectiles & Circles" (suvat toolkit, projectile independence, centripetal acceleration, range-formula code screen) → phys-forces-3 "Momentum, Collisions & Rotation" (impulse-momentum, elastic-vs-inelastic, the rotational analogs — torque/moment of inertia/angular momentum, ice-skater conservation, momentum-conservation code screen). Scoped to 2 depth lessons rather than 3 (rotational dynamics folded into phys-forces-3's back half rather than getting its own node) to keep one session's chain a real 2-lesson unit — a future phys-forces-4 on rotational dynamics alone is a legitimate later add if it feels rushed. phys-exam grew 10→14 Qs (suvat, projectile, momentum-conservation, torque numerics). Layout: chain placed at x2450/2480 (NOT straight down from phys-forces at x1980 — the existing phys-forces→robo-control edge sweeps diagonally through x1980-2070,y140-360, confirmed by the bezier-math checker, so the chain drifted right into the empty x2200-2600 corridor instead); phys-exam moved (2060,300)→(2060,480). Whole-map checker: zero edge/node crossings, zero rect overlaps. Browser-verified phys-forces-2 end-to-end (predicts → code screen live-run matching 34.6/40.0/34.6 → 5/5 quiz → mastered +100 XP → phys-forces-3 unlocked).
- ✅ **math-ode chain (DONE)**: math-ode "Differential Equations: Change Itself" (separation of variables, exponential decay/growth, Newton cooling; code screen = Euler-step decay matching e^(−kt): k=0.5→13.53, k=1.0→1.83) → math-ode-2 "Second-Order: Oscillators & Damping" (mass-spring-damper, damping ratio ζ under/critical/over, cross-links robo-control-2 — underdamped IS PID overshoot; code screen = overshoot of released mass, c=1→−0.44, c=4→0.00, c=8→0.01) → math-ode-3 "Systems, Phase Portraits & Stability" (x′=A·x, eigenvalues decide stability, phase portraits, links robo-control-3 + math-linalg-3; code screen = state evolution, stable c=+4→0.00 vs unstable c=−2→75.36). math-exam grew 14→18 Qs (decay-shape, critical-damping, eigenvalue-stability, phase-portrait). LAYOUT (this was the hard part — the math/robotics quadrant is at packing capacity): the chain could NOT be a straight vertical stack. Two long diagonal corridors (cpu→robo-sensing, math-linalg-3→robo-kinematics) cut through the pocket below the calculus column at y~630 with only a ~66px gap (< node height), and the ai-learning corridor holds x1240-1400, and math-exam's own fan-in funnels through the same region — triple-booked. The incoming edge (math-calculus-3→math-ode) wants x≥1480; the diagonal gap wants the middle node at x≤1360. Resolved by a slight ZIGZAG threading the widening part of the diagonal gap: math-ode (1420,480), math-ode-2 (1360,670), math-ode-3 (1380,790) — found via a scripted margin-maximizing search (200-sample bezier checker), verified zero crossings + zero overlaps on the live module and visually. Nodes -2/-3 sit right against the ai corridor (edges pass ~11-23px to their left) — do NOT nudge them left. math-ode browser-verified end-to-end (predicts → code screen live-run 13.53/1.83 → 5/5 quiz → mastered +100 XP → math-ode-2 unlocked).
- ✅ **math-linalg-4/-5 (DONE, 2026-07-08)**: math-linalg-4 "Rank, Column Space & Least Squares" (column space = reachable outputs, rank = independent columns, overdetermined A·x=b has no exact solution → project onto column space → normal equations AᵀA·x̂=Aᵀb; code screen = least-squares line fit through (0,1)(1,1)(2,3) via Cramer's rule, slope m=1.000 / intercept c=0.667; links math-linalg-3 + ai-learning) → math-linalg-5 "SVD & the Pseudoinverse" (A=UΣVᵀ = rotate-stretch-rotate, singular values σ grade closeness to collapse, rank = #nonzero σ, condition number σ_max/σ_min beats the determinant as a health check, pseudoinverse A⁺=VΣ⁺Uᵀ = least-squares for tall / minimum-norm for wide/redundant-arm IK; code screen = pinvDiag inverts nonzero σ and zeroes the null direction, [2,5]→0.5 0.2 / [4,0]→0.25 0; links math-linalg-3 + robo-kinematics-3 + math-linalg-4). math-exam grew 22→26 Qs (least-squares normal equations, rank-from-singular-values, redundant-arm minimum-norm pseudoinverse, ill-conditioning vs non-zero determinant). Both tier-1 Depth. LAYOUT: reserved slot was x1400 but that col is pierced by the math-logic→math-exam sweep AND grazed above by the prog-data→os-processes highway — resolved by placing at x1490, y620/835 (checker-tuned: -4 above the prog-data sweep, -5 threaded between that sweep and the math-logic→exam sweep on its left, ≥28px both). Zero crossings + zero tight-clearances in BOTH tiers. **Also fixed a pre-existing latent bug** (see gotchas): the code-runner worker injects `print` as a Function parameter, so a starter line `const print = (...a) => console.log(...)` throws "Identifier 'print' has already been declared" and the screen never runs — math-linalg-3's code screen was silently broken this way. Removed that line from -3/-4/-5. Both new code screens + the fixed -3 Node-verified against the worker's exact print semantics; -4/-5 browser-verified end-to-end (predicts → code screen live-run matching → 4/4 quiz → mastered +100 XP → next unlocked). Source: 18.06.
- ✅ **robo-kinematics chain (DONE)**: robo-kinematics-2 "Rotation Matrices & Homogeneous Transforms" (R(θ)=[[c,−s],[s,c]], det R=1 always, homogeneous transform = rotation+translation, FK = chained transform product; code screen = 2-link planar FK, straight(0,0)→5.00,0.00 / elbow(0,90)→3.00,2.00 / both(90,90)→−2.00,3.00) → robo-kinematics-3 "The Jacobian, Singularities & Inverse Kinematics" (J maps θ̇→ẋ, det(J)=0 ⇒ singularity = the det=0/gimbal-lock payoff from math-linalg-3, det(J)=L₁L₂sin θ₂, numerical IK as gradient-descent-with-the-Jacobian; code screen = det(J) scan, elbow 0°→0.00 / 90°→6.00 / 180°→0.00 showing both singularities). Both cross-link math-linalg-2/-3. robo-exam grew 14→18 Qs (FK numeric, det(J)=0 singularity, transform-chaining, numerical-IK-as-gradient-descent). LAYOUT (easy this time — unlike the ODE chain): the far-right-middle pocket (x2000-2560, y960-1350, between physics/chem above and robo-exam below) is the map's most open region, so the chain drifted right from robo-kinematics (2000,880) to robo-kinematics-2 (2260,1020) → robo-kinematics-3 (2260,1200), feeding robo-exam (2120,1400) right below. Zero crossings + zero overlaps on the FIRST placement guess (checker confirmed). robo-kinematics-2 browser-verified end-to-end (predicts → FK code screen live-run matching → 5/5 quiz → mastered +100 XP → robo-kinematics-3 unlocked).
- ✅ **robo-estimation chain (DONE)**: robo-estimation "State Estimation: Fusing Noisy Senses" (1D Kalman = Gaussian×Gaussian, Kalman gain K=σ²pred/(σ²pred+σ²meas), fused mean=pred+K(meas−pred), var always shrinks; code screen = fuse two Gaussians, equal-trust K=0.50/mean=11.00/var=2.00 vs trust-sensor K=0.90/mean=11.80/var=0.90; links math-prob-3 Bayes) → robo-estimation-2 "The Multivariate Kalman Filter" (state vector + covariance matrix P, predict grows P adds Q / update shrinks P, off-diagonals correlate variables so "observe one fix many", uncertainty ellipse; code screen = info-form repeated update, variance 10→1.67→0.91→0.63; links robo-control-3 state-space) → robo-estimation-3 "EKF & Sensor Fusion" (nonlinear models linearized each step via Jacobian F=∂f/∂x, H=∂h/∂x — ties to robo-kinematics-3 Jacobian; range-sensor H=[x/r,y/r] is the unit line-of-sight vector → trilateration/GPS; IMU+GPS+encoder fusion, EKF divergence when curvature/uncertainty high; code screen = range Jacobian at (3,4)→r=5.00,H=[0.60,0.80] and (0,5)→[0.00,1.00]). robo-exam grew 18→22 Qs (Kalman fusion numeric, dead-reckoning covariance growth, multi-sensor EKF fusion, EKF linearization). Prereq = robo-control-3 (structural; math-prob-3 Bayes is a LINK not prereq, matching the robo-control-3 convention — avoids a long cross-map edge). LAYOUT (easy — like kinematics, unlike ODE): placed as a clean vertical column at x2380, y1360/1540/1720 in the open bottom-right corner (below robo-kinematics-3, right of robo-exam which it feeds). Zero crossings + zero overlaps; the only <1px near-graze in the whole map is a PRE-EXISTING one (math-linalg-3→robo-kinematics near robo-sensing), not from this chain. robo-estimation browser-verified end-to-end (predicts → code screen live-run matching → 5/5 quiz → mastered +100 XP → robo-estimation-2 unlocked).
- ✅ **math-mv chain (DONE)**: math-mv "Partial Derivatives & the Gradient" (tier-0 intro; partials = freeze-and-differentiate, ∇f stacks them & points steepest-uphill, −∇f is downhill; code screen = central-difference gradient of x²+3y², (1,2)→2.00,12.00 / (3,−1)→6.00,−6.00; forks off math-calculus-2 since partials ARE single-var derivatives) → math-mv-2 "The Multivariable Chain Rule & the Jacobian" (df/dt = Σ ∂f/∂xᵢ·dxᵢ/dt = "sum over paths" = backprop; the Jacobian matrix of partials, det J = local area-scaling, det J=0 ⇒ singularity; code screen = polar→cartesian Jacobian det, (r=3,θ=0)→3.00 / (r=5,θ=π/2)→5.00; cross-LINKS robo-kinematics-3 + robo-estimation-3 — same Jacobian, three hats) → math-mv-3 "Optimization: Critical Points & Lagrange Multipliers" (∇f=0 min/max/saddle, Hessian/eigenvalue test links math-linalg-3, constrained ∇f=λ∇g, gradient descent; code screen = descent on x²+3y² from (2,2), 10 steps→x=0.215/y=0.000 shows the steep axis empties first, 40→0/0). math-exam grew 18→22 Qs (partial derivative, gradient direction+magnitude, numeric Jacobian at a point, Lagrange condition). LAYOUT: reserved slot x2520 (right of the calculus/ODE column) — placed math-mv/-2/-3 at (2520,400/600/800), zero crossings BOTH tiers on the first guess. math-mv browser-verified end-to-end (predict shuffled → code screen live-run matching 2.00,12.00 / 6.00,−6.00 → 4/4 quiz → mastered +100 XP → math-mv-2 unlocked).
- ☐ **math-stats chain (NEW)**: variance/covariance (the language of Kalman filters) → CLT & estimation → hypothesis tests/CIs + regression. Direct SBI-y2 Statistics prep, also founder-relevant (A/B tests). Source: 18.05 + Seeing Theory (interactive — matches the app's pedagogy).

Phase 2 — degree bar + Engineering breadth (see HANDOFF item 2 below):
- ☐ robo-control-4 (extend): transfer functions/poles → Bode/frequency response → LQR ("u=−Kx, K chosen optimally" — the natural next lesson after robo-control-3). Source: 2.004 + Brian Douglas/MATLAB Tech Talks.
- ✅ **phys-electricity chain (DONE)**: phys-electricity-2 "Kirchhoff’s Laws & the Voltage Divider" (KCL=charge conservation, KVL=energy conservation, Vout=Vin·R₂/(R₁+R₂), series adds / parallel = product-over-sum, resistive sensors ARE dividers; code screen = divider 12V·1k/2k→8.00, 1k/1k→6.00, series 3000, parallel 666.67) → phys-electricity-3 "RC Transients, Induction & the Transistor" (capacitor charges on V(t)=Vin(1−e^(−t/RC)), one time-constant≈63% — links math-ode exponential; Faraday induction = motors/generators/encoders, motor back-EMF explains stall-current inrush; transistor = signal controls load → H-bridge, the bridge to the future Electronics domain; code screen = RC charge 10V/RC2 at t=2/4/6 → 6.32/8.65/9.50). phys-exam grew 14→18 Qs (divider numeric, KCL balance, RC time-constant, motor back-EMF). robo-sensing REPOINTED cpu+phys-electricity → cpu+phys-electricity-3 (chain-bottom convention; reroutes the phys-electricity→robo-sensing edge cleanly to the chain end). LAYOUT (trivial): straight vertical drop at x1760, y320/500 — the gap between phys-electricity (1760,140) and robo-sensing (1760,760) was empty and exactly chain-sized. Zero crossings + zero overlaps. phys-electricity-2 browser-verified end-to-end (predicts → divider code screen live-run matching → 5/5 quiz → mastered +100 XP → phys-electricity-3 unlocked).
- ☐ math-calculus-4/-5 (extend): limits & continuity (informal ε-δ) → integration techniques → Taylor series. Source: 18.01SC + Paul's Notes.
- ☐ **phys-statics chain (NEW, seeds MechE domain)**: FBDs/equilibrium/trusses → stress-strain → beam bending + torsion (a chassis is a structure; motor-shaft sizing is a torsion problem). Source: MIT 2.001 + The Efficient Engineer.
- ☐ **chem-quant chain (NEW)**: stoichiometry/moles → thermochemistry & equilibrium → electrochemistry/batteries (every robot runs on one; Battery University resources). Source: 5.111SC.

Phase 3 — CS core depth:
- ✅ **prog-c chain (DONE, 2026-07-08, branch `prog-c-chain`)**: prog-c "Pointers & the Memory Model" (tier-0; memory = numbered byte array, a pointer IS an address, & vs *, stack vs heap, NULL, arrays as pointer arithmetic; code screen = memory-as-array + pointer-as-index, dereference after ptr+2 → 5 then 13, load-bearing blank in a print arg) → prog-c-2 "malloc, Structs & a Linked List" (heap vs stack lifetime, malloc/free/leak, structs, linked-list traversal code screen = pointer-chase heap[addr].next until -1/NULL → 10/20/30, and omitting the advance infinite-loops into the 2s worker timeout — a callback to prog-data-2's missing-base-case; list-vs-array incl. cache locality) → prog-c-3 "Bit Manipulation & Memory Bugs" (AND/OR/XOR/shifts, set/clear/test masks on an MCU register — code screen reg|MOTOR then reg&~MOTOR → 4/0; buffer overflow/use-after-free/dangling/double-free/leak/off-by-one, the overflow→return-address exploit, "use tools not vigilance"). prog-exam 14→18. LAYOUT: the reserved **x0 column** (y440/640/840, above prog-exam, left of the depth chain at x240) — it sits LEFT of every corridor fanning out of prog-data (net-stack→x0 passes at x>410 for y>440, db/algo further right), so zero crossings + zero tight clearances BOTH tiers on the FIRST placement guess. All 3 code screens Node-verified (completed matches, starter fails); intro browser-played end-to-end (predict → code live-run 5/13 → 4/4 quiz → mastered +100 XP → prog-c-2 unlocked). Source: CS50 wk4 + Beej's Guide to C + Stanford Bit Twiddling Hacks.
- ☐ **os-concurrency chain (NEW)**: threads & races → locks/CVs → deadlock + real-time scheduling. The most robotics-critical missing CS concept (concurrent sensor/control/comms loops). Source: OSTEP — its homework simulators auto-generate problems + answers, a perfect fit for extraPractice.
- ☐ algo-structures/algo-graphs depth (extend): sorting + divide&conquer/recurrences → Dijkstra/heaps worked problems → dynamic programming. Source: 6.006 psets/quizzes (both have solutions).
- ☐ ai-learning chain (extend): linear regression & loss → train/test/overfitting/regularization → backprop (the chain-rule lesson is the perfect prereq; Karpathy micrograd-style code screens).
- ☐ **net-sockets chain (NEW)**: sockets hands-on (UDP/TCP client-server — ROS DDS, telemetry) → TCP mechanics (handshake/retransmission/AIMD) → UDP-vs-TCP for robots + subnets/DHCP/ARP. Source: Kurose-Ross interactive problems + Beej's Guide to Network Programming.
- ☐ math-logic chain (extend, Stage-2 bridge): sets/relations/functions → induction → proof technique. Source: Book of Proof (free) + 6.042J.

Phase 4 — opportunistic completeness: theory formal-language chain (regex↔DFA equivalence, pumping lemma — thinnest free problem supply of any domain, expect to author extraPractice from scratch), security real-crypto chain (AES/RSA/TLS via Cryptopals), db normalization/B+trees (15-445 HWs), architecture assembly/memory-hierarchy chain (Nand2Tetris projects 4–6), multivariable completion (vector-calc theorems), history expansion, and giving every remaining intro lesson an extraPractice pool.

## Playable lessons so far (87 nodes — ALL 14 domains COMPLETE + module exams; math (linalg 5-deep/prob/calculus/ODE/multivariable), robo-control, robo-kinematics, robo-estimation, programming AND physics (forces + electricity) deepened into mini-courses)
- **Physics depth (Stage 1, part 4 — DONE, first item off the Cowork-sourced backlog)**: phys-forces became a 3-node chain: phys-forces (1980,0) → phys-forces-2 "Kinematics: suvat, Projectiles & Circles" (2450,150; the four suvat equations, projectile independence, centripetal acceleration; code screen proves the range formula R=v0²sin(2θ)/g and the 30°/60° tie around the 45° max) → phys-forces-3 "Momentum, Collisions & Rotation" (2480,320; impulse-momentum, elastic-vs-inelastic collisions, the rotational analogs τ/I/L, ice-skater angular-momentum conservation; code screen proves momentum conservation in an inelastic collision). Both have `deeper?` toggles + 4-question extraPractice pools; resources curated (Walter Lewin 8.01 lectures, PhET sims, Mechanical Universe). phys-exam grew 10→14 questions (suvat distance, projectile-independence, momentum-conservation, torque numerics) and now gates on all 5 physics nodes. LAYOUT NOTE (new pattern): unlike prior chains that ran straight down from the intro at the same x, this one drifted RIGHT to x2450/2480 — the pre-existing phys-forces→robo-control edge sweeps diagonally through roughly x1980-2070,y140-360 (confirmed by sampling its bezier with the math checker), which is exactly where a straight-down chain would sit. Repointing that edge wasn't an option (robo-control's PID content doesn't actually depend on the deep kinematics/rotation chain, so repointing to the chain-bottom would be pedagogically dishonest) — moving the NEW chain into the empty x2200-2600 corridor instead was the correct call. phys-exam moved (2060,300)→(2060,480) to sit below the enlarged cluster. Whole-map checker: zero edge/node crossings, zero rect overlaps. phys-forces-2 browser-verified end-to-end (predicts → code screen live-run, output `30°: 34.6` / `45°: 40.0` / `60°: 34.6` matched expected → 5/5 quiz → mastered +100 XP → phys-forces-3 unlocked).
- **Module exams (Stage 1, part 1 — DONE)**: every domain has an `isExam` node (hcw-exam + 13 new: prog/math/phys/algo/os/net/db/theory/ai/sec/robo/hist/chem-exam). Each: 1 intro + 10 FRESH quiz questions (authored against the domain lessons to avoid reuse), all `correctIndex: 0` (shuffle convention), misconception-specific explanations, numerics hand-verified. Validator ENFORCES: exactly one exam per domain, exam prereqs = all other domain nodes, ≥8 quiz questions. Map: exams sit below their cluster (redundant prereq edges auto-hidden by SkillTreeView's transitive-closure logic); History was restructured into a horizontal ROW (x2600/2840/3080, y0) so its 3 prereq-free nodes fan into hist-exam (2840,240) without edges crossing nodes. hist-exam browser-verified end-to-end (unlock → 10/10 → mastered +100 XP).
- **Math depth (Stage 1, part 2 — DONE for the 3 robotics-critical math nodes)**: linalg, probability and calculus each became a 3-lesson vertical CHAIN (intro → -2 → -3): math-linalg→math-linalg-2 (Matrices as Transformations)→math-linalg-3 (Determinants & Eigenvectors); math-prob→math-prob-2 (Distributions & Expected Value)→math-prob-3 (Bayes' Rule); math-calculus→math-calculus-2 (Derivative Rules & Chain Rule)→math-calculus-3 (Integrals & Accumulation). Each depth lesson: concept→worked-fading→independent, a `deeper?` toggle, ONE Node-verified `code` screen, and an `extraPractice` pool (the "independent problem set"). math-exam grew to 14 questions (added chain-rule, integral, determinant, expected-value Qs) and now gates on all 10 math nodes. Layout: columns at x1000/1240/1480, rows y0/190/380, math-exam moved to (900,600) fanning in from math-logic + the three -3 nodes. CRITICAL FIX: ai-learning was repointed from the math INTROS to the chain BOTTOMS (['math-prob-3','math-linalg-3']) and nudged x1240→1300 — otherwise its long down-edges (esp. the reserved x1240 math-prob→ai-learning corridor) would pierce the new depth nodes stacked below the intros; pointing at the -3 nodes makes those edges exit BELOW the chains and clear algo-graphs. (robo-kinematics still ← math-linalg intro; its right-drifting edge clears the depth column by ~24px.) When stacking future depth chains below a node that has long downstream corridors, repoint the downstream dep to the chain bottom. math-linalg-2 browser-verified end-to-end incl. its code screen (worker output `rotated: -2 3` / `twice: -3 -2` matched expected).
- **Robo-control depth (Stage 1, part 3 — DONE)**: robo-control became a 3-lesson chain: robo-control (1520,880) → robo-control-2 "Tuning PID: Reading the Response" (1400,1060; step-response metrics, symptom→gain table, Ziegler–Nichols, loop rate; code screen proves P-only steady-state error numerically: Kp=3→0.50, Kp=12→0.87) → robo-control-3 "Beyond PID: Feedforward & State Space" (1440,1240; feedforward, state vector, x′=A·x+B·u, u=−K·x, Kalman-as-Bayes teaser; code screen = 3 ticks of the state update; links to math-linalg-2 + math-prob-3). Both have `deeper?` toggles + 4-question extraPractice pools; resources curated (MATLAB Tech Talks, Z–N wiki, bzarg Kalman article). robo-exam grew to 14 questions (spec→metrics, feedforward-vs-I, state-completeness, Kalman-fusion Qs) and gates on all 7 robotics nodes. Layout: robo-exam moved (1520,1300)→(2120,1400) — RIGHT of the db column, below kinematics — so its three visible in-edges (control-3 long east edge, kinematics vertical-ish, ros short diagonal) all get clean lanes; math-exam moved (900,600)→(800,780) out of the cpu→robo-sensing / prog-data→robo-ros / prog-data→db-relational edge highways that pierced it; robo-kinematics math prereq REPOINTED math-linalg→math-linalg-3 (chain-bottom convention; pedagogically apt — det=0 IS gimbal lock) which un-pierced the math depth grid. robo-control-2 browser-played end-to-end (predicts → code screen live-run → 5/5 quiz → mastered +100 XP → robo-control-3 unlocked).
- **Map edge/node crossing checker (USE THIS for any layout change — the whole map is at ZERO crossings in every tier, keep it there)**: `node scripts/check-map-layout.mjs` — a committed script that parses curriculum.ts, rebuilds the visible edge list for EVERY view tier (same transitive filter + projectToTier re-anchoring as SkillTreeView), samples each bezier mathematically (handles (x+96,y+80)→(x+96,y), control offset d = 0.5·Δy down / 6.25·√Δ up, rects 192×80), and reports penetrations, rect overlaps and <25px clearances; exits non-zero on any hit. Screenshots miss crossings because edges render UNDER nodes — never rely on them alone. IMPORTANT: check ALL tiers — tier-0 re-anchored edges (e.g. prog-data→robo-ros) fly different paths than their tier-1 originals and have caught real hits.
- **Programming depth (Stage 1 — DONE) + foundations map finalization**: prog-data grew a chain: prog-data-2 "Recursion: Functions That Call Themselves" (360,420; base/recursive case, dive-and-return, call stack; code screen = add the missing base case, and the missing-base-case predict ties to the runner's 2s timeout) → prog-data-3 "Debugging & Testing: Code You Can Trust" (360,620; scientific-method debugging, minimal repros, off-by-ones, bisection, edge-case tests, regression tests; code screen = fix the off-by-one in sumReadings). prog-exam → 14 Qs (doubling-recursion trace, base-case-before-recursive-call, verify-the-fix discipline, recursion depth vs loops) at (160,880), gating all 5 prog nodes. REPOINTED to the chain bottom prog-data-3: os-processes, algo-bigo, net-stack AND robo-ros — the robo-ros repoint reroutes its old corridor (which passed UNDER robo-control/robo-embedded, the last known crossing) cleanly below the robotics cluster. db-relational deliberately STAYS ← prog-data (from the chain bottom its corridor would pierce robo-control-2). Map moves to clear lanes: algo-bigo (480,760)→(600,710), algo-structures (720,880)→(720,910), math-exam (800,780→ finally (800,770)). Result: whole-map ZERO edge/node crossings + zero rect overlaps (math checker). prog-data-2 browser-played end-to-end (predicts → base-case code screen live → 5/5 quiz → mastered +100 XP → prog-data-3 unlocked). Also foundations polish: all `text-white` → `text-slate-100` in games + GatePuzzleScreenView; GateSandbox colorMode + dots follow store.theme.
- How Computers Work: bits, gates, adder, cpu, hcw-exam (module exam).
- Math: math-linalg (vector playground), math-calculus (slope/derivative explorer), math-prob (law-of-large-numbers coin sim).
- Programming: prog-variables (live code runner).
- Physics: phys-forces (F=ma launch sim), phys-electricity (Ohm's-law circuit sim).
- Programming (COMPLETE): prog-variables, prog-functions, prog-data (+prog-data-2 recursion, prog-data-3 debugging/testing depth chain) — all use the live code runner.
- Math (COMPLETE): math-linalg, math-calculus, math-prob, math-logic.
- Physics (COMPLETE): phys-forces, phys-electricity, phys-energy.
- Robotics Bridge (COMPLETE): robo-sensing, robo-control (+robo-control-2, robo-control-3 depth chain), robo-kinematics, robo-embedded (reuses cpu-sim), robo-ros.
COMPLETE domains (all nodes have lessons): How Computers Work, Programming, Math, Physics, Robotics Bridge, History of Science & Tech, Chemistry, Algorithms & Data Structures.
- History (COMPLETE): hist-scientific-revolution, hist-computing (computing-timeline game), hist-industrial. All prereq-free entry points. Subject color fuchsia.
- Chemistry (COMPLETE): chem-atoms (atom-builder game) → chem-reactions → chem-materials (prereq chain). Subject color teal.
- Algorithms (COMPLETE): algo-bigo (big-o-race game + code screens) → algo-structures (3 code screens) → algo-graphs (pathfinder-sim game: BFS vs A* on a wall-drawing grid; verified BFS explores ~80 cells vs A* ~11 for the same shortest path).
- AI & ML (COMPLETE): ai-search (state-space planning, no game — predict-rich), ai-learning (gradient-descent game: converge/diverge/precision challenges on L(x)=x²), ai-neural (neural-playground game: perceptron sliders separate clusters, then hit the XOR wall — the give-up button IS the win, reveals hidden-layer fix).
- OS (COMPLETE): os-processes (round-robin code screen), os-memory (page-table translation code screen), os-io (polling vs interrupts, predict-rich).
- Networks (COMPLETE): net-stack (envelope-wrapping code screen), net-packets (routing/TTL/head-of-line predicts), net-protocols (HTTP vs MQTT pub/sub — mirrors the polling/interrupt pattern).
- Databases (COMPLETE): db-relational (foreign-key lookup code screen), db-sql (SELECT/WHERE + JOIN as loops, 2 code screens), db-transactions (indexes + ACID + lost-update race).
- Theory (COMPLETE): theory-fsm (fsm-lab game: drive a 4-state robot behavior FSM, incl. proving ignored events do nothing), theory-turing (halting problem, ties to the code-runner's 2s timeout), theory-complexity (P vs NP, ties to crypto + ai-search).
- Security (COMPLETE): sec-threats (threat models, Mirai, weakest link), sec-crypto (Caesar code screen → key space → public keys), sec-systems (least privilege, signed updates, secure boot).
ALL code-screen expected outputs are Node-verified (run the completed solution, compare to `expected`) — do this for every new code screen.
The full vertical slice Physics → Robotics → tuned PID controller is playable. robo-control unlocks once robo-sensing + phys-forces are mastered (chain: phys-forces + phys-electricity → robo-sensing → robo-control).
Games live in src/components/games/, all implement `{ onComplete(score) }`.
- PID sim (PidSim.tsx): simulation is deterministic; constants (STEPS=400, DRAG=0.8, GRAVITY=2.5, TOL=0.5) were numerically tuned so P-only and PI-without-D FAIL but P≈4/I≈2/D≈4 settles — verify any change to these still leaves it winnable (quick node script simulating the loop).

Gotchas learned:
- SVG-drag games (VectorPlayground, SlopeExplorer): wrap `setPointerCapture` in try/catch (some pointers/headless reject it and abort the drag). Drag works via the svg's onPointerMove + dragKey state.
- Batch/rapid-click games (ProbabilitySim): use FUNCTIONAL setState (setX(prev=>...)) or rapid clicks race on stale closure state; fire onComplete from useEffect, not inside the updater.
- `code` Screen kind runs learner JS in a Web Worker (blob URL) with a 2s terminate timeout — infinite-loop safe. Output captured via injected `print()`, compared line-normalized to `expected`. IMPORTANT: the worker runs the code as `new Function('print', 'console', code)`, so `print` (and `console`, aliased to it) is already a parameter — a starter that does `const print = (...a) => console.log(...)` throws "Identifier 'print' has already been declared" and the screen never runs. NEVER declare `print` in a starter; just call the injected `print(...)` (it joins args with a space, same as the old custom one). (This bug silently broke math-linalg-3's code screen until 2026-07-08. The validator now ENFORCES this — plus exam-question freshness vs same-domain lesson quizzes, non-empty code prompt/expected, and resource titles — added in the 2026-07-08 audit-polish pass.)
- Quiz/predict options are SHUFFLED at render (QuizScreenView/PredictScreenView useMemo keyed on screen) — authoring with correctIndex 0 everywhere is fine and convenient; the learner never sees a position pattern. Don't "fix" content by varying correctIndex.
- Light mode: never use `text-white` in games — use `text-slate-100` (the html.light block reverses the slate ramp, white stays white on light cards). Same for rings: `ring-white` is invisible on light — use `ring-slate-100`. In SVG games, hardcoded near-white hex (`#e2e8f0`) breaks on light canvases — use `var(--color-slate-200)` (Tailwind v4 emits the palette as CSS vars, and html.light overrides them, so SVG attrs re-theme too). React Flow MiniMap `nodeColor` receives raw hex, NOT CSS vars — it must branch on `theme` (dark: bright `subjectHex`/`#1e293b`-later; light: darker `subjectHexLight` mirror of the html.light overrides + `#cbd5e1`-later), or light mode shows black blocks. Theme-audited 2026-07-07: all `*-white` utilities, near-white SVG hex, and the minimap fixed (SkillNode/GateSandbox rings, SlopeExplorer tangent, ArmSim hand dot, SkillTreeView MiniMap); white-on-colored-fill (AtomBuilder nucleus) is fine in both themes.

## HANDOFF — read this first (written at end of the Cowork-integration session)
1. **DONE — Cowork research integrated.** `docs/curriculum-gap-analysis.md` (44KB, canonical syllabi per domain, gap rankings, live-verified problem sources & resource shortlists, suggested build order, links fetched 2026-07-06) is committed and folded into the "Stage 1 — structure & backlog" section above: the backlog is re-ranked, expanded from 5 items to a 4-phase list (Phase 1 = robotics core, Phase 2 = degree bar + Engineering, Phase 3 = CS systems trio, Phase 4 = opportunistic), and now points at the report for sourcing instead of improvising. **Honest note for Ollie**: the report is genuinely excellent (every URL live-verified, exam-with-solutions sources prioritized) but it roughly quadruples Stage 1's scope — treat Phase 1 as the real near-term queue, not all four phases as an immediate to-do. Still not yet done: pulling specific problems from the cited psets/exams INTO `extraPractice` pools and `resourcesByNode` per-lesson — that happens naturally as each chain gets built, not as a separate pass.
2. **Engineering breadth — still queued, now correctly sequenced.** "Engineering" is currently just a subject color on 2 robotics-bridge nodes (robo-embedded, robo-ros). Plan (per HANDOFF item 2 from last session, now cross-referenced with the report's Phase 2): AFTER the phys-electricity chain (Phase 2, item 2 above — the report's own Phase 2 also seeds this bridge) and phys-statics chain (Phase 2, item 4 — explicitly noted in the report as "seeds MechE domain"), add an **Electronics & Circuit Design** domain (~3 intro nodes: components&circuits → transistors&op-amps → motor drivers/H-bridge) and a **Mechanical Engineering basics** domain (~3 intro nodes: statics&loads → gears/linkages/mechanisms → materials&making), following the "new subject" checklist in the brainstorm section. Do NOT pad the current 2-node tag with filler before then.

## Next (resume here)
STATE as of last session: **Stage 0 (foundations) FINALIZED** — 82 playable nodes, ALL 14 domains complete + a module exam each. **Stage 1 (depth) STRUCTURED and underway** — chains done for math×4 (linalg/prob/calculus/ODE), robo-control, robo-kinematics, robo-estimation, programming, phys-forces, phys-electricity; spaced repetition shipped; **gating switched to SOFT**. This was a big session: folded the Cowork gap-analysis into a ranked 4-phase backlog, then built FOUR chains — math-ode (×3), robo-kinematics (×2), robo-estimation/Kalman (×3), and phys-electricity (×2) — all end-to-end with content, layout (zero crossings), grown exams, browser-verified. **The entire robotics-perception-control spine is now deep: sensing → control/tuning/state-space → kinematics/Jacobians → Kalman/EKF estimation**, and **phys-electricity now reaches transistors/H-bridge — the on-ramp to the planned Electronics domain (HANDOFF item 2)**. Robotics-bridge is 12 nodes; physics is 7. Committed on `main`.

AFTER the content sessions, a UX round shipped (user-requested): app renamed **Atlas**, roadmap button toggles, the stage-scalable **view-tier selector** (🌱 Essentials / 🌳 Depth — see Feature notes for how future stages plug in), the Progress tab (stats + 14 achievements + reset), achievement toasts, and a theme audit incl. the light-mode MiniMap fix. All browser-verified in both themes. NOTE: the PREVIEW browser's localStorage profile is test data and got reshuffled during toast verification (~10 mastered showing) — irrelevant to any real user profile; reset it freely.

To restart the dev server: `export PATH="$HOME/.local/node/bin:$PATH"` then `cd ~/Claude/Projects/"Learning website"` then `npm run dev` → open the printed http://localhost:5173.

**MAP LAYOUT REFACTOR SHIPPED (2026-07-07, Ollie-requested "reorganize the map logically") — the math-quadrant blocker is GONE.** The whole map was re-laid-out to the "streets" design (see Conventions bullet + the header comment in curriculum.ts): HCW moved next to Physics (killed the cpu→robo-sensing mega-corridor that cut the math pocket), every domain got a roomy cell, robotics is a proper convergence cell bottom-right, DB moved out from under robotics to a sensible leaf column, and EVERY queued chain now has a documented reserved slot. `scripts/check-map-layout.mjs` (new, committed) verifies zero edge/node crossings in BOTH view tiers — run it after any node add/move. minZoom lowered 0.15→0.08 so Whole-map fits the bigger canvas. Progress was reset to a fresh profile at Ollie's request (he hadn't started yet). Browser-verified: validator ✓, whole map + street region + Essentials tier + both themes screenshotted.

**PREREQ CLEANUP SHIPPED (2026-07-07):** os-processes/algo-bigo/net-stack/robo-ros were repointed from the depth-chain bottom prog-data-3 back to the honest intro node prog-data (they need intro programming, not recursion+debugging — the -3 pointer was a layout-routing artifact Ollie caught). Follow-through layout: OS cluster grid-searched down to (3900,1300) so cpu→os clears the robotics cell, prog depth chain nudged to x240. Zero crossings both tiers. This removed the "a Depth lesson sits between two Foundations lessons" oddity in Depth view.

**math-mv CHAIN SHIPPED (2026-07-07) — see the ✅ backlog entry above for full detail.** Multivariable calculus (partials/gradient → chain rule/Jacobian → optimization/Lagrange), 3 lessons at x2520, math-exam 18→22, all green + browser-verified.

**math-linalg-4/-5 CHAIN SHIPPED (2026-07-08) — see the ✅ backlog entry above for full detail.** Rank/column-space/least-squares (-4) → SVD/pseudoinverse (-5), 2 lessons at x1490 (y620/835), math-exam 22→26, plus a pre-existing code-runner `print`-redeclaration bug fixed in -3/-4/-5. All green + browser-verified. Currently 87 playable nodes, 24 tier-1 Depth lessons across 12 chains.

**v2 GAP-ANALYSIS BATCH (2026-07-08) — ALL 4 CHAINS SHIPPED ✅.** `docs/curriculum-gap-analysis-v2.md` specced a four-chain batch; all four are now built, committed and browser-verified end-to-end (predict → code live-run matching → quiz → mastered +100 XP) on branch `build-chains`, each in its own commit:
- ✅ **math-stats** (×3): variance/covariance → CLT/√n estimation → hypothesis tests/regression. At x2040 y1300/1490/1680 (the v2 reserved x2520 slot was gone — math-mv took it and the prog-data→robo-ros corridor saturates the mid-map; found a clean slot in the below-corridor street via a margin-max bezier search). math-exam 26→30.
- ✅ **robo-control-4** (single node): poles → Bode → LQR (scalar-LQR Riccati code screen). At (2950,2300). robo-exam 22→26 (also fixed its stale intro count).
- ✅ **Electronics** (NEW subject `electronics`, orange): elec-components → elec-transistors → elec-motors + elec-exam. Full new-subject checklist wired (Subject type, SkillNode styles/names, SkillTreeView hex+minimap hexLight, index.css orange-300). At x4660 y1420/1600/1780 (exam 4520,1960) — dropped low so the phys-electricity-3→elec-components corridor sags below phys-exam.
- ✅ **phys-statics** (×3): FBDs/equilibrium (reactions code screen 75/25, 50/50, 25/75) → stress/strain & material limits (σ=F/A, ΔL; 100MPa/1mm vs 50MPa/0.5mm) → bending & torsion (σ=6M/bh²; 6.0→1.5 MPa, the h² depth lever). Subject `physics` (amber). At x4740 y260/440/620 (shifted right of phys-energy so BOTH tier-0 and tier-1 incoming edges clear it). phys-exam 18→22. Seeds a future MechE domain. Source: MIT 2.001 + The Efficient Engineer + engineeringstatics.org.

Map is at ZERO crossings in BOTH view tiers throughout; every code screen Node-verified. **Now ~97 playable nodes.**

**Note on the v2 doc:** it only ever contained §1 (Electronics) and §2 (math-stats) — §3 (robo-control-4) and §4 (phys-statics) were never written, so those two were built from v1 gap-analysis + CLAUDE.md backlog + domain knowledge (all sources still real & cited in `resourcesByNode`).

**AUDIT-POLISH PASS SHIPPED (2026-07-08, Ollie-requested "go over everything and make it better").** Five parallel audits (core components/store, games, lesson content ×2, data layer). Content came back CLEAN — zero factual/numeric errors across all 98 lessons, all code-screen expecteds verified. Fixes applied: StrictMode-safe rAF loops in ForceSim/PidSim (rAF was scheduled inside setState updaters → multiplying frames), onComplete latches/effects in AdcSim/ArmSim/OhmsLawSim/BinaryCounter/BinaryConverter (was firing inside updaters or unlatched), CodeScreenView worker+timeout cleanup on unmount, importProgress now type-validates every field (malformed JSON can't corrupt state), Escape-to-close on all header panels + Roadmap (new shared `useDismissable` hook; Roadmap got role=dialog), shared Fisher-Yates in `src/shuffle.ts`, projectToTier guards empty prereqIds (was a module-load white-screen risk), jump-to select is now a repeatable action ("Pick a domain…" placeholder), ExtraPractice memoizes its quiz screen (no mid-question reshuffle), achievements edge-case guards (empty-domain/zero-exam counts, Deep Diver works for any chain length), 4 new validator invariants (see gotchas), math-linalg-2/-3 got curated resources (URLs live-verified via oEmbed). All games browser-verified still completable (ForceSim/PidSim/AdcSim/BinaryCounter played end-to-end). Known-and-accepted: PathfinderSim step-3 wall check is cheesable (learner only cheats themselves); SVG drag handles have no keyboard fallback; "reviews done" stat can dip after a failed review (any derivable-only definition has this; no new persisted counters allowed).

Next priorities:
1. **Merge the two outstanding chain branches → `main`** (each off `main`, independently mergeable; both touch curriculum.ts + lessons/index.ts + the domain exam, so expect small CLAUDE.md/index conflicts if merged back-to-back): **`os-concurrency-chain`** (OS: threads/races → locks/semaphores → deadlock/real-time, ×3) and **`prog-c-chain`** (this branch — Programming: pointers → malloc/linked-list → bits/memory-bugs, ×3). The v2 `build-chains` batch is ALREADY merged (commit 6c81526).
2. Deploy to Vercel (manual steps above) for a permanent/mobile URL.
3. **Engineering breadth follow-through:** Electronics is live; a **Mechanical Engineering basics** domain (statics&loads → gears/linkages → materials&making) is the natural next new-subject — phys-statics already seeds it. Also queued: chem-quant (stoichiometry → thermochemistry → electrochemistry/batteries). (prog-c AND os-concurrency are now DONE — see backlog.)
4. Spaced-repetition follow-ups when bored of content: cumulative cross-domain review exams, gentle mastery-decay visuals.

**Branch note:** the v2 batch (`build-chains`) is merged to `main`. TWO newer chains are each on their own branch off `main`, NOT yet merged: `os-concurrency-chain` (OS ×3) and `prog-c-chain` (Programming ×3, this branch). Merge/PR both when ready. (Node count is ~97 on `main`; ~100 after os-concurrency merges; ~103 after prog-c too.)

Bigger arc (see "Roadmap to bachelor depth" above): after breadth, deepen each node (Stage 1) → rigor/proofs (Stage 2) → labs/projects (Stage 3) → spaced-repetition retention (Stage 4) → cross-domain capstones + business track (Stage 5).

Nice-to-haves: extract a lesson-generation prompt template; give GateSandbox `colorMode` from theme (still hardcoded dark); deploy to Vercel for a permanent/mobile URL (manual steps above — gh CLI not installed).
